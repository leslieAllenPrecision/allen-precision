<odoo>
    <record id="res_partner_view_buttons" model="ir.ui.view">
        <field name="name">res.partner.view.buttons</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="priority" eval="3"/>
        <field name="groups_id" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>

        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <field name='approval_needed' invisible="1"/>
                    <button name="button_approve" string="Approve Customer" type="object" class="btn btn-primary" attrs="{'invisible':[('approval_needed','=',False)]}"  groups="customer_approval.group_approval_manager"/>
                </header>
            </xpath>
        </field>
    </record>




</odoo>